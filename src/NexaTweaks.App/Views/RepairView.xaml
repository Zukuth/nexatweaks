<UserControl x:Class="NexaTweaks.App.Views.RepairView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NexaTweaks.App.ViewModels">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:RepairActionCardViewModel}">
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,10">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" Style="{StaticResource SectionHeaderText}" FontSize="13.5"/>
                                <Border CornerRadius="8" Padding="7,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{StaticResource SafeBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Risk}" Value="Advanced">
                                                    <Setter Property="Background" Value="{StaticResource AdvancedBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Risk}" Value="Risky">
                                                    <Setter Property="Background" Value="{StaticResource RiskyBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding RiskLabel}" FontSize="9.5" FontWeight="Bold" Foreground="White"/>
                                </Border>
                                <TextBlock Text="{Binding EstimatedDuration}" Style="{StaticResource CaptionText}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Description}" Style="{StaticResource BodyText}" Margin="0,6,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="Ejecutar" Style="{StaticResource SecondaryButton}" VerticalAlignment="Center"
                                Command="{Binding DataContext.RunCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolConverter}}"/>
                    </Grid>
                    <TextBlock Text="Ejecutando..." Foreground="{StaticResource AccentCyanBrush}" FontSize="11" Margin="0,10,0,0"
                               Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    <Border Background="{StaticResource BgWindowBrush}" CornerRadius="6" Padding="10" Margin="0,10,0,0"
                            Visibility="{Binding Output, Converter={StaticResource NullToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock FontSize="11" FontWeight="Bold" Margin="0,0,0,4">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="✓ Completado"/>
                                        <Setter Property="Foreground" Value="{StaticResource SafeBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding LastSuccess}" Value="False">
                                                <Setter Property="Text" Value="✗ Terminó con errores"/>
                                                <Setter Property="Foreground" Value="{StaticResource RiskyBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Text="{Binding Output}" FontFamily="Consolas" FontSize="10.5" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="32,28,32,20">
            <TextBlock Text="Reparación" Style="{StaticResource TitleText}"/>
            <TextBlock Text="Herramientas oficiales de Windows para diagnosticar y reparar archivos de sistema, red y Update, todo desde un mismo lugar." Style="{StaticResource BodyText}" Margin="0,6,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="32,0,32,28" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Actions}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
